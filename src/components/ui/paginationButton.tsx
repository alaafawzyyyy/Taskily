export default function PaginationButton({ number }: { number: string }) {
  return <div className="flex justify-center items-center w-8 h-8 rounded-sm border-[1px] border-[#C3C6D64D]">{number}</div>;
}
